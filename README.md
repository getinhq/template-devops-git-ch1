# Git repository basics

## Task
Initialize a git repo, add a README, create a branch, commit, and push to GitHub.

## Starter
Edit `notes.txt` with what you did. CI only checks that required files exist.
